import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin: 8,
        flexDirection: 'row',
        justifyContent: 'space-between',
    },
    todoTitle: {
        fontSize: 32,
        color: 'orange',
        fontWeight: 'bold',
    },
    todoCounter: {
        fontSize: 32,
        color: 'orange',
    },
});